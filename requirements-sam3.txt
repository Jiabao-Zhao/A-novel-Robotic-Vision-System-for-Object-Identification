# Local automatic masks, tested in the existing WSL CUDA environment.
# Install the CUDA torch build separately, not the Windows CPU-only build.
torch==2.11.0
torchvision==0.26.0
transformers==5.5.4
safetensors>=0.4.3
Pillow>=10
